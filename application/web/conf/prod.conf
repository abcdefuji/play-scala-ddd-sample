# This is the PRODUCTION configuration file for the appliation/admin.
# ~~~~~

# It includes first the shared.prod.conf and web.conf files
include "shared.prod"
include "web"

# Include here any specific configuration for ADMIN subproject in production

this.file="web -> prod.conf"

# For production override the secret value
# See http://www.playframework.com/documentation/latest/ApplicationSecret for more details.