# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Add Subdomain Routing

# Delegate routes to sub-projects

->  / admin.Routes

->  / api.Routes

->  / web.Routes